<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sinest.gw_1000">

    <uses-sdk android:minSdkVersion="14"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-feature android:name="android.hardware.wifi.direct" android:required="true"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar"
        android:name=".management.Application_communicator">

        <activity
            android:name=".splash.Activity_booting"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".mode.Activity_waiting_rfid"
            android:screenOrientation="landscape"
            android:theme="@style/AppTheme" />

        <activity
            android:name=".mode.Activity_library"
            android:screenOrientation="landscape" />

        <activity android:name=".mode.Activity_waiting" />

        <activity android:name=".mode.Activity_manual_mode_setting" />

        <activity
            android:name=".mode.Activity_manual_mode_pattern_popup"
            android:screenOrientation="landscape"
            android:theme="@android:style/Theme.Dialog" />

        <activity android:name=".mode.Activity_waiting_working_time_popup"
            android:screenOrientation="landscape"
            android:theme="@android:style/Theme.Dialog"/>

    </application>

</manifest>
